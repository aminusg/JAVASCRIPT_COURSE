export default function () {
    return '<div>Logo</div>';
  }
  
  export function multiply(numOne, numTwo) {
    return numOne * numTwo;
  }
  
  export const greeting = 'Hi there';
//En un mismo archivo podemos tener varios tipos de exportaciones, No es buenos tener varios default en el mismo, pero sí de distinto tipo